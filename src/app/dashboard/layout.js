export const metadata = {
  title: 'Seller Studio',
  description: 'Create and manage your Anya AI storefront.',
  robots: { index: false, follow: false },
};

export default function DashboardLayout({ children }) {
  return children;
}
